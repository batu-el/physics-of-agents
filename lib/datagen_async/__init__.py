"""Fully asynchronous (schedule-driven) dataset generation: make_schedule
(firing-schedule CLI), dynamics_seq (sequential-event dynamics), collect_energy_seq
(CLI). Setup and dynamics primitives are shared with the synchronous ``datagen``."""
